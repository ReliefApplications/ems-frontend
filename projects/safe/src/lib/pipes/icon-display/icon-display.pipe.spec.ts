import { SafeIconDisplayPipe } from './icon-display.pipe';

describe('SafeIconDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeIconDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
