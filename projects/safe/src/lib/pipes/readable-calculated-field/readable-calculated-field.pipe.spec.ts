import { ReadableCalculatedFieldPipe } from './readable-calculated-field.pipe';

describe('ReadableCalculatedFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableCalculatedFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
