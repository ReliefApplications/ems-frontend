/**
 * Enum with all the icon variants we accepted by our custom icon component
 */
export enum IconVariant {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SUCCESS = 'success',
  DANGER = 'danger',
  LIGHT = 'light',
}
