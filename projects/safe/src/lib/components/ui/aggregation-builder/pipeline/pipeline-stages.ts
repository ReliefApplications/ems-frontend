/* eslint-disable @typescript-eslint/naming-convention */
/*  Enum of StageType.
 */
export enum StageType {
  FILTER = 'filter',
  SORT = 'sort',
  GROUP = 'group',
  ADD_FIELDS = 'addFields',
  UNWIND = 'unwind',
  CUSTOM = 'custom',
}
