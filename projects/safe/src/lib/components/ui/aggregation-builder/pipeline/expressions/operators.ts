/**
 * Enum of available accumulators, for field creation.
 */
export enum Accumulators {
  SUM = 'sum', //field
  AVG = 'avg', //field
  COUNT = 'count',
  MAX = 'max', //field
  MIN = 'min', //field
  FIRST = 'first', //field
  LAST = 'last', //field
}
