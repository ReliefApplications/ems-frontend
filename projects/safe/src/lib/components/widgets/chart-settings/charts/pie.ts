import { Chart } from './chart';

/**
 * Class for the pie chart
 */
export class Pie extends Chart {
  /**
   * Constructor of the Pie chart
   *
   * @param settings Settings for the pie chart
   */
  constructor(settings?: any) {
    super(settings);
  }
}
