import { NavigationTabDirective } from './navigation-tab.directive';

describe('NavigationTabDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationTabDirective();
    expect(directive).toBeTruthy();
  });
});
