import { NavigationTabsDirective } from './navigation-tabs.directive';

describe('NavigationTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationTabsDirective();
    expect(directive).toBeTruthy();
  });
});
