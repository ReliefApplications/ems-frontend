import { FormWrapperDirective } from './form-wrapper.directive';

describe('FormWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new FormWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
