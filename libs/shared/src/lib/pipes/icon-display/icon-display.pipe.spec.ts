import { IconDisplayPipe } from './icon-display.pipe';

describe('IconDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new IconDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
