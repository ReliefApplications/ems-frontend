import { FileColorPipe } from './file-color.pipe';

describe('FileColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FileColorPipe();
    expect(pipe).toBeTruthy();
  });
});
