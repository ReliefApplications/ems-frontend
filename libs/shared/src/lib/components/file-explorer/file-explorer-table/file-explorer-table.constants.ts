import { PagerSettings } from '@progress/kendo-angular-grid';

/** Settings for pager */
export const PAGER_SETTINGS: PagerSettings = {
  buttonCount: 5,
  type: 'numeric',
  info: true,
  previousNext: true,
};
