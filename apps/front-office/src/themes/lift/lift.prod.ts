/**
 * Default theme for expertise environments.
 * Theming defines colors of the modules, and additional parameters that can appear on screen.
 */
export const theme = {
  headerColor: 'primary',
  prefix: null,
  primary: '#1E2DBE',
};
