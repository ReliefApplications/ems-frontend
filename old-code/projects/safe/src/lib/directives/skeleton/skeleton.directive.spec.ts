import { SafeSkeletonDirective } from './skeleton.directive';

describe('SafeSkeletonDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeSkeletonDirective();
    expect(directive).toBeTruthy();
  });
});
